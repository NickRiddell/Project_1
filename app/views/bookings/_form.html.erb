<%= form_for [@show, @booking] do |f| %>

  <div class="booking-form">
    <%= f.label :ticket %>
    <%= f.number_field :ticket%>
    <%= f.hidden_field :show_id, value: @show.id%>
  <br>
  <%= f.submit "create booking" %>
<% end %>
</div>